//memory module
module bram_memory(
    input clk,
    input we,
    input [7:0] addr,
    input [31:0] din,
    output reg [31:0] dout
);

reg [31:0] memory [0:255];

always @(posedge clk) begin
    if (we)
        memory[addr] <= din;

    dout <= memory[addr];
end

endmodule
